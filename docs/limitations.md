# Limitations

- The MVP uses simple TF-IDF retrieval, so it may miss semantically relevant but lexically different evidence.
- The skill extraction module is rule-based and should be expanded gradually.
- The system does not verify whether a GitHub project is fully reproducible.
- The system should not be used to fabricate resume claims.
- The current version is a local decision-support tool, not an automated application system.
